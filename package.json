{
  "name": "grocery-list",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "My Grocery List — vanilla PWA. No build step; this manifest exists only for the test toolchain.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "happy-dom": "^15.11.6",
    "vitest": "^2.1.8"
  }
}
